# Memos

- [ ] 2026-06-30: Pre-game lobby `/table?table=` now flashes "loading" for ~1.4s then help (was instant help). `isPotentialTablePage` accepts it, so resolveContent runs the loading message + extractFromTab's retry loop on a board-less page. Fix: bail to help immediately when no frame has a board-shaped slug (lobby), only retry/loading when a board frame exists but isn't ready. (review wc0jsqv7f, LOW)
- [ ] 2026-06-30: `autohide-game` pin mode never auto-closes on unsupported game tables in the modern `/tableview` layout — `shouldAutoClose` judges the slug-less shell URL via `classifyNavigation` (returns showHelp → stays open). Fix: make the autohide-game close decision frame-aware (close when the probed board's gameName is not in SUPPORTED_GAMES; leave a not-yet-resolved board open). (review wc0jsqv7f, LOW; opt-in mode, default is pinned)
- [ ] 2026-06-30: Real board-frame extraction errors (missing player color, "no notification data", ajaxcall error — all after gameui is confirmed) are now swallowed as "not a game"/help instead of surfacing as `gameError`/ERR badge on the click + live paths. `pickExtraction` skips any `{error}` frame → extractFromTab returns false → notAGame. Fix: give extract.ts a `notGame` vs `error` discriminator; extractFromTab throws on a real board error (restoring gameError), returns false only when every frame was genuinely not-a-board. (review wc0jsqv7f, LOW; transient/self-healing)
